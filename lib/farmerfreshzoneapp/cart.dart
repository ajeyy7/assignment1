import 'package:flutter/material.dart';

class Cartff extends StatelessWidget {
  const Cartff({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
body:
Center(child: Text("CART")),
    );
  }
}
