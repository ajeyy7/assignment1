import 'package:flutter/material.dart';

class Accountff extends StatelessWidget {
  const Accountff({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ACCOUNTS")),

    );
  }
}
